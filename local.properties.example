mcp.servers=weather,trains

mcp.servers.weather.baseUrl=http://127.0.0.1:8081/mcp
mcp.servers.weather.required=true

mcp.servers.trains.baseUrl=http://127.0.0.1:8082/mcp
mcp.servers.trains.required=true

agent.pipeline.enabled=true
agent.pipeline.maxIterations=12

# Старый single-server формат всё ещё поддерживается, если mcp.servers не задан:
# mcp.transport=stdio
# mcp.stdio.command=java
# mcp.stdio.args=-Dkotlin-logging.logStartupMessage=false -Dorg.slf4j.simpleLogger.logFile=System.err -Dorg.slf4j.simpleLogger.defaultLogLevel=warn -jar McpWeatherStationServer-1.0.0-all.jar
# mcp.stdio.cwd=/path/to/weather-station-server

# Если сервер собран в remote/http-режиме:
# mcp.transport=http
# mcp.server.url=http://127.0.0.1:8081/mcp

# Для диалога с LLM задайте переменные окружения:
# export OPENAI_API_KEY=...
# export OPENAI_MODEL=gpt-5
# Не храните API-ключ в local.properties.
